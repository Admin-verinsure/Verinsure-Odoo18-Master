<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Log List View -->
    <record id="view_auto_reconciliation_log_list" model="ir.ui.view">
        <field name="name">auto.reconciliation.log.list</field>
        <field name="model">auto.reconciliation.log</field>
        <field name="arch" type="xml">
            <list string="Reconciliation Logs" default_order="create_date desc" decoration-success="state=='done'" decoration-danger="state=='failed'" decoration-warning="state=='running'">
                <field name="name"/>
                <field name="company_id"/>
                <field name="triggered_by"/>
                <field name="bank_matched"/>
                <field name="customer_matched"/>
                <field name="vendor_matched"/>
                <field name="intercompany_matched"/>
                <field name="total_matched"/>
                <field name="state" widget="badge"
                    decoration-success="state == 'done'"
                    decoration-danger="state == 'failed'"
                    decoration-warning="state == 'running'"/>
                <field name="create_date" string="Run Date"/>
            </list>
        </field>
    </record>

    <!-- Log Form View -->
    <record id="view_auto_reconciliation_log_form" model="ir.ui.view">
        <field name="name">auto.reconciliation.log.form</field>
        <field name="model">auto.reconciliation.log</field>
        <field name="arch" type="xml">
            <form string="Reconciliation Log" create="false" edit="false">
                <header>
                    <field name="state" widget="statusbar" statusbar_visible="running,done,failed"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="company_id"/>
                            <field name="triggered_by"/>
                            <field name="create_date" string="Run Date"/>
                        </group>
                        <group string="Match Summary">
                            <field name="bank_matched"/>
                            <field name="customer_matched"/>
                            <field name="vendor_matched"/>
                            <field name="intercompany_matched"/>
                            <field name="total_matched"/>
                        </group>
                    </group>
                    <group string="Notes">
                        <field name="notes" nolabel="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Log Action -->
    <record id="action_auto_reconciliation_log" model="ir.actions.act_window">
        <field name="name">Reconciliation Logs</field>
        <field name="res_model">auto.reconciliation.log</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
