<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!-- Login page: adjust 'Don't have an account?' -->
        <template id="login" inherit_id="auth_signup.login" name="Rotary Signup Links">
            <!-- Replace sign-up link -->
            <xpath expr="//a[@t-if='signup_enabled']" position="replace">
                <a t-if="signup_enabled" t-attf-href="/web/is_member?{{ keep_query() }}">Don't have an account?</a>
            </xpath>
        </template>

        <!-- Landing page: Are you a member? -->
        <template id="signup_is_member" name="Sign up Selection">
            <t t-call="web.login_layout">
                <style>
                    .custom-question {
                        font-size: 24px;
                        font-weight: bold;
                        margin-bottom: 10px;
                    }
                    .custom-button {
                        width: 100%;
                        font-size: 18px;
                        padding: 10px;
                    }
                </style>
                <div class="container mt-5">
                    <div class="row">
                        <div class="col-12 text-center">
                            <p class="custom-question">Are you a Rotary International Member?</p>
                        </div>
                    </div>
                    <div class="row justify-content-center">
                        <div class="col-12 col-md-3 mb-2">
                            <a href="/web/signup" class="btn btn-success custom-button">Yes</a>
                        </div>
                        <div class="col-12 col-md-3 mb-2">
                            <a href="/web/signup_non_member" class="btn btn-primary custom-button">No</a>
                        </div>
                    </div>
                </div>
            </t>
        </template>

        <!-- Member Signup -->
        <template id="signup" name="Rotary Member Signup">
            <t t-call="web.login_layout">
                <form class="oe_signup_form" role="form" method="post" t-if="not message">
                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                    <t t-call="rotary_signup.fields">
                        <t t-set="only_passwords" t-value="bool(token and not invalid_token)"/>
                    </t>

                    <p class="alert alert-danger" t-if="error" role="alert">
                        <t t-esc="error"/>
                    </p>
                    <input type="hidden" name="redirect" t-att-value="redirect"/>
                    <input type="hidden" name="token" t-att-value="token"/>

                    <div class="text-center oe_login_buttons pt-3">
                        <button type="submit" class="btn btn-primary btn-block">Sign up</button>
                        <a t-attf-href="/web/login?{{ keep_query() }}" class="btn btn-link btn-sm" role="button">Already have an account?</a>
                        <div class="o_login_auth"/>
                    </div>
                </form>
            </t>
        </template>

        <!-- Non-member Signup -->
        <template id="signup_non_member" name="Rotary Non-member Signup">
            <t t-call="web.login_layout">
                <form class="oe_signup_form" role="form" method="post" t-if="not message">
                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                    <t t-call="rotary_signup.non_registered_fields">
                        <t t-set="only_passwords" t-value="bool(token and not invalid_token)"/>
                    </t>

                    <p class="alert alert-danger" t-if="error" role="alert">
                        <t t-esc="error"/>
                    </p>
                    <input type="hidden" name="redirect" t-att-value="redirect"/>
                    <input type="hidden" name="token" t-att-value="token"/>

                    <div class="text-center oe_login_buttons pt-3">
                        <button type="submit" class="btn btn-primary btn-block">Sign up</button>
                        <a t-attf-href="/web/login?{{ keep_query() }}" class="btn btn-link btn-sm" role="button">Already have an account?</a>
                        <div class="o_login_auth"/>
                    </div>
                </form>
            </t>
        </template>

        <!-- Common Fields (Member) -->
        <template id="fields" name="Rotary Signup Fields">
            <div class="form-group field-name">
                <label for="first_name">First Name</label>
                <input type="text" name="first_name" t-att-value="first_name" id="first_name"
                       class="form-control form-control-sm" placeholder="e.g. John"
                       required="required"/>
            </div>

            <div class="form-group">
                <label for="last_name">Last Name</label>
                <input type="text" name="last_name" t-att-value="last_name" id="last_name"
                       class="form-control form-control-sm" placeholder="e.g. Smith"
                       required="required"/>
            </div>

            <div class="form-group">
                <label for="email">Email</label>
                <input type="text" name="email" t-att-value="email" id="email"
                       class="form-control form-control-sm" placeholder="e.g. example@example.com"
                       required="required"/>
            </div>

            <div class="form-group">
                <label for="rotary_id">Rotary International Number (5–8 digits)</label>
                <input type="number" name="rotary_id" t-att-value="rotary_id" id="rotary_id"
                       class="form-control form-control-sm" placeholder="e.g. 83314"
                       required="required" min="10000" max="99999999"/>
            </div>

            <t t-if="program_types">
                <div class="form-group">
                    <label for="program_type">Program Type</label>
                    <select name="program_type_id" id="program_type" class="form-control form-control-sm" required="required">
                        <option value="">-- Select Program Type --</option>
                        <t t-foreach="program_types" t-as="ptype">
                            <option t-att-value="ptype.id" t-esc="ptype.name"/>
                        </t>
                    </select>
                </div>
            </t>

            <div class="form-group">
                <label for="rotary_club_id">Club Name</label>
                <select name="rotary_club_id" class="form-control form-control-sm" required="required">
                    <option value="">-- Select a Club Name --</option>
                    <t t-foreach="clubs" t-as="club">
                        <option t-att-value="club.id" t-esc="club.name"/>
                    </t>
                </select>
            </div>

            <div class="form-group field-password pt-2">
                <label for="password">Password</label>
                <input type="password" name="password" id="password"
                       class="form-control form-control-sm" required="required"/>
            </div>

            <div class="form-group field-confirm_password">
                <label for="confirm_password">Confirm Password</label>
                <input type="password" name="confirm_password" id="confirm_password"
                       class="form-control form-control-sm" required="required"/>
            </div>
        </template>

        <!-- Common Fields (Non-member) -->
        <template id="non_registered_fields" name="Rotary Non-member Signup Fields">
            <div class="form-group field-name">
                <label for="first_name">First Name</label>
                <input type="text" name="first_name" t-att-value="first_name" id="first_name"
                       class="form-control form-control-sm" placeholder="e.g. John"
                       required="required"/>
            </div>

            <div class="form-group">
                <label for="last_name">Last Name</label>
                <input type="text" name="last_name" t-att-value="last_name" id="last_name"
                       class="form-control form-control-sm" placeholder="e.g. Smith"
                       required="required"/>
            </div>

            <div class="form-group">
                <label for="email">Email</label>
                <input type="text" name="email" t-att-value="email" id="email"
                       class="form-control form-control-sm" placeholder="e.g. example@example.com"
                       required="required"/>
            </div>

            <t t-if="program_types">
                <div class="form-group">
                    <label for="program_type">Program Type</label>
                    <select name="program_type_id" id="program_type" class="form-control form-control-sm">
                        <option value="">-- Select Program Type --</option>
                        <t t-foreach="program_types" t-as="ptype">
                            <option t-att-value="ptype.id" t-esc="ptype.name"/>
                        </t>
                    </select>
                </div>
            </t>

            <div class="form-group field-password pt-2">
                <label for="password">Password</label>
                <input type="password" name="password" id="password"
                       class="form-control form-control-sm" required="required"/>
            </div>

            <div class="form-group field-confirm_password">
                <label for="confirm_password">Confirm Password</label>
                <input type="password" name="confirm_password" id="confirm_password"
                       class="form-control form-control-sm" required="required"/>
            </div>
        </template>

    </data>
</odoo>
