<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_akahu_credential_replace_wizard_form" model="ir.ui.view">
            <field name="name">akahu.credential.replace.wizard.form</field>
            <field name="model">akahu.credential.replace.wizard</field>
            <field name="arch" type="xml">
                <form string="Configure Akahu App Credentials">
                    <sheet>
                        <group>
                            <field name="credential_id" invisible="1"/>
                            <field name="new_app_token" widget="password" placeholder="app_token_xxxxxxxxxxxxxxxxxxxxxxx"/>
                            <field name="new_app_secret" widget="password" placeholder="your-akahu-app-secret"/>
                            <field name="new_oauth_redirect_uri" placeholder="https://your-domain.example.nz/nz_bank_reconciliation/oauth/api_redirect"/>
                        </group>
                    </sheet>
                    <footer>
                        <button name="action_save"
                                string="Save"
                                type="object"
                                class="btn-primary"
                                data-hotkey="q"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

    </data>
</odoo>
