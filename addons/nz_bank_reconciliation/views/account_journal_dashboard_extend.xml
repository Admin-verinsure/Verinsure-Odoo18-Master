<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ===================================================================
         1. ACCOUNTING DASHBOARD — Kanban card buttons (unchanged from working version)
         =================================================================== -->
    <record id="view_account_journal_dashboard_akahu" model="ir.ui.view">
        <field name="name">account.journal.dashboard.kanban.akahu</field>
        <field name="model">account.journal</field>
        <field name="inherit_id" ref="account.account_journal_dashboard_kanban_view"/>
        <field name="arch" type="xml">

            <!-- Load Akahu fields into the kanban dataset -->
            <xpath expr="//field[@name='bank_account_id']" position="after">
                <field name="has_akahu"/>
                <field name="akahu_last_synced"/>
                <field name="akahu_bank_feeds_disabled"/>
            </xpath>

            <!-- Add Akahu buttons in bank journal actions -->
            <xpath expr="//div[@name='bank_cash_buttons']" position="inside">

                <!-- Configure Bank Account button: shown when NO Akahu account is set up yet -->
                <t t-if="!record.has_akahu.raw_value">
                    <button name="action_configure_akahu_account"
                            type="object"
                            class="btn btn-primary">
                        <i class="fa fa-bank me-1"/>
                        Configure Bank Account
                    </button>
                </t>

                <!-- Fetch Transactions + last-sync info: shown when Akahu IS configured -->
                <t t-if="record.has_akahu.raw_value">

                    <!-- Re-configure button (secondary, always visible when configured) -->
                    <button name="action_configure_akahu_account"
                            type="object"
                            class="btn btn-secondary"
                            title="Edit Akahu bank account configuration">
                        <i class="fa fa-cog me-1"/>
                        Bank Account
                    </button>

                    <!-- Fetch Transactions button -->
                    <button name="action_fetch_akahu_transactions"
                            type="object"
                            class="btn btn-primary">
                        <i class="fa fa-refresh me-1"/>
                        Fetch Transactions
                    </button>

                    <t t-if="record.akahu_last_synced.raw_value">
                        <span class="text-muted ms-2 small">
                            Last Sync:
                            <t t-esc="record.akahu_last_synced.value"/>
                        </span>
                    </t>

                </t>

            </xpath>

        </field>
    </record>

    <!-- ===================================================================
         2. JOURNAL FORM — Akahu radio option + conditional banner
         New: bank_statements_source now has 'Akahu (NZ Open Banking)' option
         injected via _get_bank_statements_available_sources() override.
         The banner appears below the radio group when Akahu is selected.
         =================================================================== -->
    <record id="view_account_journal_form_akahu_feeds" model="ir.ui.view">
        <field name="name">account.journal.form.akahu.feeds</field>
        <field name="model">account.journal</field>
        <field name="inherit_id" ref="account.view_account_journal_form"/>
        <field name="arch" type="xml">

            <!--
                Inject akahu_bank_feeds_disabled as a hidden field so it can
                drive visibility of the banner. Placed after bank_account_id
                which is always present on bank journals.
            -->
            <xpath expr="//field[@name='bank_account_id']" position="after">
                <field name="akahu_bank_feeds_disabled" invisible="1"/>
            </xpath>

            <!--
                Place the Akahu info banner directly AFTER the bank_statements_source
                radio widget. It is visible only when the user selects the
                'Akahu (NZ Open Banking)' radio option.

                Using bank_statements_source == 'akahu' directly (Odoo 18 expression
                syntax) — no need to reference the computed field here.
            -->
            <xpath expr="//field[@name='bank_statements_source']" position="after">
                <div class="alert alert-info d-flex align-items-center mt-2 mb-0"
                     role="alert"
                     invisible="bank_statements_source != 'akahu'">
                    <i class="fa fa-plug fa-lg me-3"/>
                    <div>
                        <strong>Akahu Bank Feed Active.</strong>
                        Transactions are fetched automatically via the Akahu (NZ Open Banking) integration.
                        The default Odoo Bank Feeds options are inactive while Akahu is selected.
                    </div>
                </div>
            </xpath>

        </field>
    </record>

</odoo>
