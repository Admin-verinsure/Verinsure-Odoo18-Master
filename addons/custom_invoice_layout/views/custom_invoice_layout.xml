<odoo>
  <data>

    <!-- 1. Custom External Layout -->
    <template id="external_layout_invoice_standard" name="Custom External Layout">
      <t t-name="custom_invoice_layout.external_layout_invoice_standard">
        <t t-call="web.external_layout">
          <t t-set="header">
            <div class="d-flex justify-content-between align-items-center mb-2">
              <img t-if="company.logo"
                   class="o_company_logo_small"
                   t-att-src="image_data_uri(company.logo)"
                   alt="Logo"/>
              <div t-if="company.report_header"
                   t-field="company.report_header"
                   class="o_company_tagline fw-bold text-primary"/>
            </div>
          </t>
        </t>
      </t>
    </template>

    <!-- 2. Override Generic Invoice -->
    <template id="custom_invoice_layout_inherit"
              inherit_id="account.report_invoice_document">
      <xpath expr="//t[@t-set='external_layout']" position="replace">
        <t t-set="external_layout">custom_invoice_layout.external_layout_invoice_standard</t>
      </xpath>
    </template>

    <!-- 3. Override NZ Invoice -->
    <template id="custom_invoice_layout_inherit_nz"
              inherit_id="l10n_nz.report_invoice_document">

      <!-- Title override -->
      <xpath expr="//t[@t-set='layout_document_title']" position="replace">
        <t t-set="layout_document_title">
          <span t-if="not proforma"/>
          <span t-else="">PROFORMA</span>
          <span t-if="o.move_type == 'out_invoice' and o.state == 'posted'">Tax Invoice</span>
          <span t-elif="o.move_type == 'out_invoice' and o.state == 'draft'">Draft Tax Invoice</span>
          <span t-elif="o.move_type == 'out_invoice' and o.state == 'cancel'">Cancelled Tax Invoice</span>
          <span t-elif="o.move_type == 'out_refund' and o.state == 'posted'">Tax Credit Note</span>
          <span t-elif="o.move_type == 'out_refund' and o.state == 'draft'">Draft Tax Credit Note</span>
          <span t-elif="o.move_type == 'out_refund' and o.state == 'cancel'">Cancelled Tax Credit Note</span>
          <span t-elif="o.move_type == 'in_refund'">Tax Vendor Credit Note</span>
          <span t-elif="o.move_type == 'in_invoice'">Tax Vendor Bill</span>
          <span t-if="o.name != '/'" t-field="o.name"/>
        </t>
      </xpath>

      <!-- Layout override -->
      <xpath expr="//t[@t-set='external_layout']" position="replace">
        <t t-set="external_layout">custom_invoice_layout.external_layout_invoice_standard</t>
      </xpath>
    </template>

  </data>
</odoo>
