/** Backend helper: (optional) could add hints/validation in builder UI later */
odoo.define('zt_form_builder_dynamic.field_dynamic_source_backend', [], function (require) {
    "use strict";
    // Intentionally minimal - server-side constraints enforce compatibility.
});
