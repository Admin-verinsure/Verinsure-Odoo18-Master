<odoo>
    <!-- Portal: only own applications -->
    <record id="rule_youth_app_portal_own" model="ir.rule">
        <field name="name">Portal: own youth applications</field>
        <field name="model_id" ref="model_youth_volunteer_application"/>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="domain_force">[('partner_id','=',user.partner_id.id)]</field>
    </record>

    <!-- Portal: write only while in draft (locks editing after submit) -->
    <record id="rule_youth_app_portal_write_draft_only" model="ir.rule">
        <field name="name">Portal: write only draft youth applications</field>
        <field name="model_id" ref="model_youth_volunteer_application"/>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="False"/>
        <field name="domain_force">[('partner_id','=',user.partner_id.id), ('state','=','draft')]</field>
    </record>

    <!-- Privacy model: no portal access via access rights (none provided). -->
</odoo>
