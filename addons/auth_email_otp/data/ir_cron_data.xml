<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!--
            Scheduled action: cleanup expired OTP challenge records.
            Runs daily. Deletes records older than 30 days regardless of state.

            model_id: resolved via search= on ir.model to avoid the
            auto-generated XML id timing problem on fresh installs.
        -->
        <record id="ir_cron_cleanup_otp_challenges" model="ir.cron">
            <field name="name">Auth OTP: Cleanup Expired Challenges</field>
            <field name="model_id" search="[('model', '=', 'auth.otp.challenge')]"/>
            <field name="state">code</field>
            <field name="code">model.cleanup_expired_records()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall" eval="(datetime.now() + timedelta(days=1)).strftime('%Y-%m-%d 02:00:00')"/>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="user_id" ref="base.user_root"/>
        </record>

    </data>
</odoo>
