<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <template id="custom_external_layout_standard" inherit_id="web.external_layout_standard">
        <xpath expr="//div[hasclass('header')]" position="replace">
            <div class="row">
                <div style="width:50%;">
                    <div class="d-flex justify-content-start">
                        <img t-if="company.logo"
                             t-att-src="image_data_uri(company.logo)"
                             style="max-height:40px;max-width:140%"
                             alt="Logo"/>
                    </div>
                </div>
            </div>
            <div>
                <t t-if="company.report_header">
                    <div style="width:50%;">
                        <div class="d-flex justify-content-start">
                            <div t-esc="company.report_header"/>
                        </div>
                    </div>
                </t>
            </div>
        </xpath>

        <xpath expr="//div[@name='company_address']" position="replace">
            <div class="row">
                <div style="width:50%;">
                    <div itemprop="address" class="address">
                        <ul class="list-unstyled">
                            <li t-if="company.is_company_details_empty">
                                <t t-esc="company.partner_id"
                                   t-options='{"widget": "contact", "fields": ["address", "name"], "no_marker": true}'/>
                            </li>
                            <li t-else="">
                                <t t-esc="company.company_details"/>
                            </li>
                            <li t-if="forced_vat">
                                <t t-esc="company.country_id.vat_label or 'Tax ID'"/>:
                                <span t-esc="forced_vat"/>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </xpath>
    </template>
    
    <template id="custom_external_layout_boxed" inherit_id="web.external_layout_boxed">
        <xpath expr="//div[@id='boxed_header']" position="replace">
            <div id="boxed_header">
                <div class="row">
                    <div style="width:50%;">
                        <div class="d-flex justify-content-start">
                            <img t-if="company.logo"
                                 t-att-src="image_data_uri(company.logo)"
                                 style="max-height:40px;max-width:140%"
                                 alt="Logo"/>
                        </div>
                    </div>
                </div>
                <div>
                    <t t-if="company.report_header">
                        <div style="width:50%;">
                            <div class="d-flex justify-content-start">
                                <div t-esc="company.report_header"/>
                            </div>
                        </div>
                    </t>
                </div>
                <div class="row">
                    <div style="width:50%;">
                        <div itemprop="address" class="address">
                            <ul class="list-unstyled">
                                <li t-if="company.is_company_details_empty">
                                    <t t-esc="company.partner_id"
                                       t-options='{"widget": "contact", "fields": ["address", "name"], "no_marker": true}'/>
                                </li>
                                <li t-else="">
                                    <t t-esc="company.company_details"/>
                                </li>
                                <li t-if="forced_vat">
                                    <t t-esc="company.country_id.vat_label or 'Tax ID'"/>:
                                    <span t-esc="forced_vat"/>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </xpath>
    </template>
    
    <template id="custom_external_layout_bold" inherit_id="web.external_layout_bold">
        <xpath expr="//div[hasclass('bold_header')]" position="replace">
            <div class="bold_header">
                <div class="row">
                    <div style="width:50%;">
                        <div class="d-flex justify-content-start">
                            <img t-if="company.logo"
                                 t-att-src="image_data_uri(company.logo)"
                                 style="max-height:40px;max-width:140%"
                                 alt="Logo"/>
                        </div>
                    </div>
                </div>
                <div>
                    <t t-if="company.report_header">
                        <div style="width:50%;">
                            <div class="d-flex justify-content-start">
                                <div t-esc="company.report_header"/>
                            </div>
                        </div>
                    </t>
                </div>
                <div class="row">
                    <div style="width:50%;">
                        <div itemprop="address" class="address">
                            <ul class="list-unstyled">
                                <li t-if="company.is_company_details_empty">
                                    <t t-esc="company.partner_id"
                                       t-options='{"widget": "contact", "fields": ["address", "name"], "no_marker": true}'/>
                                </li>
                                <li t-else="">
                                    <t t-esc="company.company_details"/>
                                </li>
                                <li t-if="forced_vat">
                                    <t t-esc="company.country_id.vat_label or 'Tax ID'"/>:
                                    <span t-esc="forced_vat"/>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </xpath>
    </template>
</odoo>