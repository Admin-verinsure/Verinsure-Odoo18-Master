<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data noupdate="0">

    <!-- Login label tweak -->
    <template id="rotary_web_login" inherit_id="web.login">
      <xpath expr="//label[@for='login']" position="replace">
        <label for="login">Username</label>
      </xpath>
      <xpath expr="//input[@name='login']" position="attributes">
        <attribute name="placeholder">Username</attribute>
      </xpath>
    </template>

    <!-- Replace links on auth page -->
    <template id="login" inherit_id="auth_signup.login" name="LDAP Reset Password">
      <!-- 'Reset Password' -->
      <xpath expr="//a[@t-if='reset_password_enabled']" position="replace">
        <a t-if="reset_password_enabled" t-attf-href="/web/reset_ldap_password?{{ keep_query() }}">Reset Password</a>
      </xpath>
      <!-- 'Don't have an account?' -->
      <xpath expr="//a[@t-if='signup_enabled']" position="replace">
        <a t-if="signup_enabled" t-attf-href="/web/is_member?{{ keep_query() }}">Don't have an account?</a>
      </xpath>
    </template>

    <!-- Minimal reset page (phase 1) -->
    <template id="template_otp" name="Reset LDAP Password Form OTP">
      <t t-call="web.login_layout">
        <form class="oe_reset_password_form" method="post">
          <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
          <div class="form-group field-login">
            <label for="login" class="col-form-label">Your Username:</label>
            <input type="text" name="login" t-att-value="login" id="login"
                   class="form-control" autofocus="autofocus" required="required" autocapitalize="off"/>
          </div>
          <div class="clearfix oe_login_buttons">
            <button type="submit" class="btn btn-primary btn-block">Confirm</button>
            <div class="d-flex justify-content-between align-items-center small mt-2">
              <a href="/web/login">Back to Login</a>
            </div>
          </div>
        </form>
      </t>
    </template>

    <!-- Invitation email (kept — referenced by res_users) -->
    <record id="invitation_email" model="mail.template">
      <field name="name">LDAP Invitation Email</field>
      <field name="model">res.users</field>
      <field name="subject">Welcome to ${object.company_id.name}</field>
      <field name="email_from">${ctx.get('email_from')}</field>
      <field name="partner_to">${object.partner_id.id}</field>
      <field name="lang">${object.lang}</field>
      <field name="auto_delete" eval="True"/>
      <field name="body_html"><![CDATA[
        <!-- keep your original email body here -->
      ]]></field>
    </record>

    <!-- Reset email -->
    <record id="reset_ldap_password" model="mail.template">
      <field name="name">Reset LDAP Password Email</field>
      <field name="model">res.users</field>
      <field name="subject">${ctx.get('subject')}</field>
      <field name="email_from">${ctx.get('email_from')}</field>
      <field name="partner_to">${object.partner_id.id}</field>
      <field name="body_html"><![CDATA[
        <!-- keep your original email body here; include ${ctx.get('otp_code')} where needed -->
      ]]></field>
    </record>

    <!-- OTP entry (phase 2) -->
    <template id="template_otp_entry" name="OTP Entry Form">
      <t t-call="web.login_layout">
        <style>
          .strength { height:5px;width:100%;margin-top:5px; }
          .strength.weak { background-color:red; }
          .strength.medium { background-color:yellow; }
          .strength.strong { background-color:green; }
          #password_error, #confirm_password_error { color:red;font-size:12px;margin-top:5px; }
          #password-rules { font-size:.8rem;color:#666;margin-bottom:10px; }
          #password-rules ul { padding-left:20px;margin-bottom:0; }
          #password-rules li { margin-bottom:5px; }
        </style>

        <t t-if="error_message">
          <div class="alert alert-danger" role="alert"><t t-esc="error_message"/></div>
        </t>
        <t t-if="password_error">
          <div class="alert alert-danger" role="alert"><t t-esc="password_error"/></div>
        </t>

        <form class="oe_reset_password_form" method="post">
          <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
          <input type="hidden" name="login" t-att-value="login"/>

          <div class="form-group">
            <label class="col-form-label">Your Username:</label>
            <input type="text" class="form-control" t-att-value="login" readonly="readonly"/>
          </div>

          <div class="form-group">
            <label for="otp" class="col-form-label">Enter One Time Password:</label>
            <input type="text" name="otp" id="otp" class="form-control" required="required" autofocus="autofocus"/>
          </div>

          <div class="form-group">
            <label for="new_password" class="col-form-label">New Password:</label>
            <div id="password-rules" class="password-rules">
              <strong>Password Rules:</strong>
              <ul>
                <li>At least 10 characters long</li>
                <li>At least 1 lowercase letter</li>
                <li>At least 1 uppercase letter</li>
                <li>At least 1 number</li>
                <li>At least 1 special character (!@#$%^&amp;*(),.?&quot;:{}|&lt;&gt;])</li>
              </ul>
            </div>
            <input type="password" name="new_password" id="new_password" class="form-control" required="required"/>
            <div id="password_strength" class="strength"></div>
            <div id="password_error"></div>
          </div>

          <div class="form-group">
            <label for="confirm_password" class="col-form-label">Confirm Password:</label>
            <input type="password" name="confirm_password" id="confirm_password" class="form-control" required="required"/>
            <div id="confirm_password_error"></div>
          </div>

          <button type="submit" class="btn btn-primary btn-block">Confirm</button>
        </form>

        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/zxcvbn/4.4.2/zxcvbn.js"></script>
        <script src="/ldap_reset_password/static/src/js/password_validation.js"></script>
      </t>
    </template>

    <!-- Final screens & misc (unchanged IDs) -->
    <template id="thanks" name="Reset LDAP Password Thanks">
      <t t-call="web.login_layout">
        <div class="clearfix oe_login_buttons">
          <form class="oe_reset_password_form">
            <p id="thanksMessage" t-esc="message"/>
            <div class="d-flex justify-content-between align-items-center small mt-2">
              <a href="/web/login">Back to Login</a>
              <a href="/web/reset_ldap_password">Reset Password</a>
            </div>
          </form>
        </div>
      </t>
    </template>

    <template id="error" name="Reset LDAP Password Error">
      <t t-call="web.login_layout">
        <div class="clearfix oe_login_buttons">
          <form class="oe_reset_password_form">
            <p id="errorMessage" t-esc="message"/>
            <div class="d-flex justify-content-between align-items-center small mt-2">
              <a href="/web/login">Back to Login</a>
              <a href="/web/reset_ldap_password">Reset Password</a>
            </div>
          </form>
        </div>
      </t>
    </template>

    <template id="portal_error" name="Reset LDAP Password Error">
      <t t-call="web.login_layout">
        <div class="clearfix oe_login_buttons">
          <form class="oe_reset_password_form">
            <p id="errorMessage" t-esc="message"/>
            <div class="d-flex justify-content-between align-items-center small mt-2">
              <a href="/my/home">Back to My Portal</a>
              <a href="/my/security">Change Password</a>
            </div>
          </form>
        </div>
      </t>
    </template>

    <template id="portal_thanks" name="Reset LDAP Password Portal Thanks">
      <t t-call="web.login_layout">
        <div class="clearfix oe_login_buttons">
          <form class="oe_reset_password_form">
            <p id="thanksMessage" t-esc="message"/>
            <div class="d-flex justify-content-between align-items-center small mt-2">
              <a href="/my/home">Back to My Portal</a>
              <a href="/my/security">Change Password</a>
            </div>
          </form>
        </div>
      </t>
    </template>

    <template id="web_error" name="Reset LDAP Password Error">
      <t t-call="web.login_layout">
        <div class="clearfix oe_login_buttons">
          <form class="oe_reset_password_form">
            <p id="errorMessage" t-esc="message"/>
            <div class="d-flex justify-content-between align-items-center small mt-2">
              <a href="/web/login">Back to Login</a>
              <a href="/web/reset_ldap_password">Change Password</a>
            </div>
          </form>
        </div>
      </t>
    </template>

    <template id="web_thanks" name="Reset LDAP Password Web Thanks">
      <t t-call="web.login_layout">
        <div class="clearfix oe_login_buttons">
          <form class="oe_reset_password_form">
            <p id="thanksMessage" t-esc="message"/>
            <div class="d-flex justify-content-between align-items-center small mt-2">
              <a href="/web/login">Back to Login</a>
              <a href="/web/reset_ldap_password">Change Password</a>
            </div>
          </form>
        </div>
      </t>
    </template>

    <!-- Used by controller -->
    <template id="template_invalid_login" name="Invalid Login">
      <t t-call="web.login_layout">
        <div class="alert alert-danger" role="alert">
          The username you entered was not found.
        </div>
        <div class="mt-2">
          <a href="/web/reset_ldap_password" class="btn btn-light btn-sm">Try Again</a>
          <a href="/web/login" class="btn btn-link btn-sm">Back to Login</a>
        </div>
      </t>
    </template>

    <template id="template_contact_admin" name="Contact Admin">
      <t t-call="web.login_layout">
        <div class="alert alert-warning" role="alert">
          We couldn't find an email for your account. Please contact an administrator to proceed with password reset.
        </div>
        <div class="mt-2">
          <a href="/web/login" class="btn btn-link btn-sm">Back to Login</a>
        </div>
      </t>
    </template>

    <!-- Backend tree view override -->
    <record id="change_password_wizard_user_tree_view" model="ir.ui.view">
      <field name="name">Change Password Users</field>
      <field name="model">change.password.user</field>
      <field name="inherit_id" ref="base.change_password_wizard_user_tree_view"/>
      <field name="arch" type="xml">
        <list string="Users" editable="bottom" create="false" delete="false">
          <field name="user_id" column_invisible="True"/>
          <field name="user_login" force_save="1"/>
          <field name="new_passwd" password="True"/>
        </list>
      </field>
    </record>

    <!-- Portal security (kept as-is) -->
    <template id="portal_my_security">
      <t t-call="portal.portal_layout">
        <!-- (keep your original portal security form body) -->
      </t>
    </template>

    <record id="view_users_form_inherit" model="ir.ui.view">
      <field name="name">res.users.preferences.form.inherit</field>
      <field name="model">res.users</field>
      <field eval="18" name="priority"/>
      <field name="inherit_id" ref="base.view_users_form_simple_modif"/>
      <field name="arch" type="xml">
        <xpath expr="//button[@name='preference_change_password']" position="replace">
          <button name="preference_change_password" type="button" string="Change password"
                  class="btn btn-danger" onclick="window.location.href='/web/reset_ldap_password'"/>
        </xpath>
      </field>
    </record>

    <!-- Server Action to clear signup URLs -->
    <record id="action_clear_signup_urls" model="ir.actions.server">
      <field name="name">Clear Signup URLs</field>
      <field name="model_id" ref="base.model_res_users"/>
      <field name="state">code</field>
      <field name="code">model.clear_signup_urls()</field>
    </record>

    <record id="action_res_users_clear_signup_urls" model="ir.actions.act_window">
      <field name="name">Clear Signup URLs</field>
      <field name="res_model">res.users</field>
      <field name="view_mode">list</field>
      <field name="binding_model_id" ref="base.model_res_users"/>
      <field name="binding_view_types">list</field>
      <field name="groups_id" eval="[(4, ref('base.group_no_one'))]"/>
    </record>

    <record id="action_res_users_clear_signup_urls_binding" model="ir.actions.client">
      <field name="name">Clear Signup URLs</field>
      <field name="tag">action</field>
      <field name="target">new</field>
      <field name="binding_model_id" ref="base.model_res_users"/>
      <field name="params" eval="{'action_id': ref('ldap_reset_password.action_clear_signup_urls')}"/>
    </record>

  </data>
</odoo>
