<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <!-- Backend ticket form — show Program Type and Club fields for agents -->
  <record id="helpdesk_ticket_view_form_program_club" model="ir.ui.view">
    <field name="name">helpdesk.ticket.form.program.club</field>
    <field name="model">helpdesk.ticket</field>
    <field name="inherit_id" ref="helpdesk.helpdesk_ticket_view_form"/>
    <field name="arch" type="xml">
      <xpath expr="//field[@name='team_id']" position="after">
        <field name="hd_program_type_id" optional="show"/>
        <field name="hd_club_id" optional="show"/>
      </xpath>
    </field>
  </record>

  <!-- res.partner form — let admin assign a program type to a club/partner -->
  <record id="res_partner_view_form_program_type" model="ir.ui.view">
    <field name="name">res.partner.form.helpdesk.program.type</field>
    <field name="model">res.partner</field>
    <field name="inherit_id" ref="base.view_partner_form"/>
    <field name="arch" type="xml">
      <xpath expr="//field[@name='category_id']" position="after">
        <field name="helpdesk_program_type_id" optional="show"/>
      </xpath>
    </field>
  </record>

</odoo>
